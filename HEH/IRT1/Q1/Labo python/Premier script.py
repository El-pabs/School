a = input ("de quelle couleur est votre haut ? :" )
ak = "jaune"
bk = "pull"
if (a) == ak:
    b = input ("est-ce un pull ou un t-shirt ? :")
    if (b) == bk:
        print ("vous avez donc un", b, "de couleur", a)
    else: print ("ok")
else :
    print ("m'en fou alors")
